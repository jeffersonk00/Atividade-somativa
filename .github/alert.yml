name: Discord Alert

on:
  push:
    branches: [ main ]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send Discord message
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "🚀 Novo push no repositório!"}' \
          ${{ secrets.DISCORD_WEBHOOK }}name: Discord Alert

on:
  push:
    branches: [ main ]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send Discord message
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "🚀 Novo push no repositório!"}' \
          ${{ secrets.DISCORD_WEBHOOK }}