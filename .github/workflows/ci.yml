name: Build

on:
  push:
    branches: [ main, desenvolvimento, feature-ci-cd ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Run Script
      run: |
        echo "Build executando com sucesso"